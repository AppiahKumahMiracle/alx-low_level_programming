#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main -  print number from 0 to 10.
 * Return: 0 (Run successful).
 *
 */

int main(void)
{
	int count;

	while (count < 10)
	printf("%d", count);
	count++;
	printf('\n');

	return (0);
}
